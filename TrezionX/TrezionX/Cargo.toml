[package]
name = "TrezionX"
version = "1.0.0"
edition = "2021"
build = "build.rs"

[dependencies]
reqwest = { version = "0.11", features = ["blocking", "native-tls"] }
rand = "0.8"
colored = "2.0"
socket2 = "0.5"
hex = "0.4"

[build-dependencies]
winres = "0.1"
